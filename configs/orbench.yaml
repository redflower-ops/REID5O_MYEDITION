project:
  name: "reid5o_myedition"
  device: "cpu"
  seed: 42

data:
  dataset_name: "ORBench"
  root: "./data/ORBench"
  modalities: ["rgb"]
  batch_size: 8
  num_workers: 0

train:
  epochs: 1
  lr: 0.0003

eval:
  topk: 10

infer:
  topk: 10
